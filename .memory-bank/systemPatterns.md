# System Patterns

## Format
- [YYYY-MM-DD]: Decision -> Reason -> Alternative considered

## Decisions
- [2026-07-04]: Vertical-slice layout (shared/ + features/ + command.py) -> Mirrors codeq/smart-trim patterns; cohesion over size -> Flat single-file rejected (1244L monolith)
- [2026-07-04]: Role-based model routing via `_ROLE_MODEL_MAP` -> Best model per task type; extensible via env vars -> Flat candidate list (no task-type awareness)
- [2026-07-04]: `shared/compat.py` for sys.path bootstrap -> Symlink-safe, absolute path resolution -> Relative `__file__` resolution (breaks on symlinks)
- [2026-07-04]: Hook package (shim + pip install -e) -> Preserves wired path in settings.json -> CLI console script (wrong pattern for a hook)
- [2026-07-04]: `# vs-soft-allow` for improve.py nesting -> for/try/if is natural shape of retry loops over model APIs -> Refactoring would reduce readability
- [2026-07-04]: Commit + push to `origin/main` is the DEFAULT closing step for verified work in this project (not gated on a per-task explicit request) -> User pref "recuerda commits y push"; remote = github.com/heldigard/prompt-improve; overrides the global "commit/push only when asked" default -> Waiting for explicit ask each time (friction). Still confirm first for hard-to-reverse ops (force-push, history rewrite, visibility change).
- [2026-07-04]: Project facts/conventions live in THIS `.memory-bank/` (cross-CLI: Claude/Codex/Gemini), NOT in `~/.claude/projects/.../memory/` (Claude-agent-only) -> User reminder "este proyecto tiene su propia memory bank"; keeps Codex/Gemini in sync -> Claude agent-memory dir (invisible to other CLIs)
- [2026-07-04]: `OllamaRequestError`/`OllamaUnavailable` split in shared `~/.claude/scripts/ollama_client.py:_post` -> distinguishes model-specific HTTP failure (load/OOM/404 → try NEXT fallback) from daemon-down (URLError/timeout → ABORT chain). Old code lumped `HTTPError` into `OllamaUnavailable`, so ONE model that failed to load (common: 16GB VRAM + ~50 models) killed the entire fallback chain → cloud PAYG or nothing. Subclass relationship keeps it backward-compatible (existing `except OllamaUnavailable` still catches both). Also fixed in shared `chat_fallback`/`generate_fallback`; consumed in prompt-improve `_run_ollama_models`. -> catch-and-abort on any error (lost availability on every primary load failure)
- [2026-07-04]: `<|channel>thought\n<channel|>` leak strip added to `ollama_client._strip_think_tags` (source fix, all callers) -> Gemma-4 abliterated/uncensored variants (incl. Huihui, the deep_bench improve #1) leak a thinking channel even with `think:false`; deep_bench's leak detector only caught `<think>`, so it false-ranked Huihui #1. Stripping at source makes Huihui a legit winner. -> per-caller strip duplication
- [2026-07-04]: Default improve chain = deep_bench 2026-07-04 winners (Huihui-abliterated 4.5 → Qwopus3.5:9b 2.91 → crow:9b 2.73 → qwen3.5:4b 2.19 anchor), validated through real `clean_rewrite` -> old default `batiai/gemma4-12b:q4` was a previously-deleted quant-loser (REPORT L165), never a winner; three-way mismatch (memory said Mobius, code said batiai, bench said Huihui). Chain is prioritization only — `choose_ollama_model_for_role` appends the full available-model tail, so missing models degrade gracefully. -> single hardcoded "best" model (fragile under VRAM contention)
- [2026-07-04]: Ollama UNIFIED on WSL — single server. Two daemons (WSL + Windows `ollama app.exe`) fought for the same 16GB GPU = VRAM contention root cause (16/54 smoke http_errors, "models erroneously deleted" by agents seeing the wrong store). Evidence: WSL daemon (pid 76670) was the one serving `:11434` all along (NOT Windows as user believed) — proven via `/proc/<pid>/exe`. WSL store = superset (64 models); Windows = near-subset (12). GPU parity TOTAL (same driver 595.97 / CUDA 13.2 / full 16GB via `/dev/dxg`). Updated WSL 0.23.2→0.31.1 (fixed LFM leak + `<|channel>` at source). Migrated the 1 Windows-unique model (`zfujicute/OmniCoder-Qwen3.5-9B`) via content-addressed blob copy + stripped misleading manifest `from` field. Uninstalled Windows ollama + deleted both Windows stores (`/mnt/c/Users/heldi/.ollama`). systemd override runs daemon as `eldi` + `OLLAMA_MODELS=/home/eldi/.ollama/models` (installer default `User=ollama` read an empty `/usr/share/ollama` store). -> migrating to Windows (slower drvfs, no advantage) / leaving both running (contention).
- [2026-07-04]: prompt-improve timeout + chain-cap robustness -> `OLLAMA_TIMEOUT` 20→45s (a 12B primary needs ~25-30s cold load + 600-token rewrite); fallback timeout cap 8/10→30s; chain capped to 6 models (`([primary]+fallbacks)[:6]`) so a hung daemon can't walk the full 60+ tail before cloud fallback. Paired with the harness `TimeoutError→OllamaRequestError` (continue, not abort) — a slow cold-loading primary now falls through to the fast qwen3.5:4b anchor. -> 20s + abort-on-timeout (e2e `call_ollama_rewrite` returned None because Huihui primary timed out cold and the chain died).
- [2026-07-05]: Target-aware prompt profiles are part of the prompt-improve contract -> The LLM that improves the prompt is not necessarily the agent/model that receives it; Claude, Codex/OpenAI GPT, Antigravity/Gemini, and proxy models need different prompt structure. `features/target.py` classifies CLI/model/version from hook payload/env (`PROMPT_IMPROVE_TARGET_*`, `ANTHROPIC_MODEL`, `CLAUDE_AGENT_IDENTITY`, `CODEX_MODEL`, `AGY_MODEL`) and injects family guidance into `_build_messages`. Cache mode includes the target profile so Claude XML output is not reused for Codex/Gemini. -> One universal Markdown prompt (worked, but left quality on the table and polluted cache across CLIs).
- [2026-07-05]: Refactored `features/target.py` (288L file) → `features/target/` package split by responsibility: `profile.py` (the HOW — detection/classification) + `shape.py` (the WHAT — `FamilyShape` registry with format+behavior per family, dict dispatch replacing the if/elif chain) + `__init__.py` (stable re-exports, zero diff for rules.py/improve.py). Two dimensions per family: **format** (XML/Markdown/component-blocks — stable) and **behavior** (failure-mode mitigation from `~/.claude/rules/model-specific.md` — higher churn). Different reasons of change but same dispatch key (`family`) and same consumer (`target_guidance`), so they live as two FIELDS of one registry entry, not two modules. -> 3 modules (profile/guidance/behavior) would split one cohesive "per-family knowledge" table; per-family folders (target/families/<x>.py) would be over-engineering for ~11 families × short strings.
- [2026-07-05]: Fixed semantic bug — 6 families (qwen, deepseek, glm, minimax, kimi, mimo) previously shared the IDENTICAL `_literal_guidance` text despite opposite failure-modes (qwen blind-retries failed cmds; glm loses PATH; minimax exploration-loops; kimi forces its model on subagents). The `style` field was assigned per family but UNUSED in guidance dispatch. Now each of the 11 families emits DISTINCT format+behavior guidance. `style` kept on TargetProfile (label + cache_key ingredient) but still NOT a dispatch key. Behavior mitigations are MODE-INDEPENDENT (appended in both rewrite and clarify). Cost: +30–70 tok/invocation. -> keeping the collapsed `_literal_guidance` (left real mitigations on the table; detected families correctly but shaped for none).
- 2026-07-07T21:19:20Z | Decision 2026-07-07: prompt-improve must shape prompts around tools actually available to the target CLI; Codex paths should mention codeq/codescan, not LSP.
- 2026-07-07T21:28:35Z | Target profile detection now treats explicit model identity as higher precedence than CLI fallback, so provider models launched through Codex (for example MiniMax-M3, Kimi K2.7 Code, DeepSeek V4) keep their own family guidance instead of being shaped as OpenAI/GPT.
- 2026-07-07T21:32:04Z | Proxy CLI aliases are valid target-family signals when no explicit model id is available: mini/minimax -> minimax, kimi/kimic -> kimi, dseek/deepseek -> deepseek, qwen/qwenc -> qwen, zai/glm -> glm, mimo -> mimo. Explicit model identity still has higher precedence.
- 2026-07-09T19:42:47Z | Native Claude Code UserPromptSubmit payloads may omit active-model metadata and do not reliably propagate CLAUDECODE into hook subprocesses. Target detection therefore uses the Claude-specific `transcript_path` payload field, with CLAUDECODE/CLAUDE_CODE_* retained as wrapper fallbacks; generic fields such as `permission_mode` are not sufficient to claim a CLI.
- 2026-07-15 | The complete improvement attempt shares one absolute wall-clock deadline (`OLLAMA_IMPROVE_TOTAL_TIMEOUT`, default 20s) across cloud/local routing and rewrite→clarify fallback. Per-model/cloud timeouts are capped by the remaining budget, keeping the hook below the surrounding pipeline's 22s child ceiling while preserving deterministic rule fallback. -> Per-route 24s budgets could reset and be killed by the parent before fail-open output.
- 2026-07-15 | Project-memory hints are an untrusted context boundary: reads are limited to <=64 KiB, must remain inside a non-symlinked `.memory-bank`, and fail open on path/read errors. -> Unbounded reads and escaping symlinks could inject unrelated local content into an LLM prompt.
- 2026-07-15 | The tracked `prompt-improve.py` is the single shim source of truth; the live Claude path symlinks to it, with Codex/Gemini chaining through Claude and a drift test guarding content. -> Independent live copies drift silently.
- 2026-07-12T13:04:05Z | status:completed | Import-time optional configuration is fail-open: malformed/non-finite numeric overrides use established defaults; finite cache TTL <=0 keeps cache disabled; Ollama URL normalization is total, HTTP-loopback-only, and preserves bracketed IPv6. This prevents env drift from aborting the hook before command.main().
- [2026-07-13]: Ecosystem-level skill shaping in prompt improvement -> Detects technology keywords (Angular, React, Svelte, Vue, Tailwind, CSS, Python, JS/TS, Git, Memory Bank, Spring Boot/JPA, NestJS, Browser Automation, Kubernetes, and Java) in the user's prompt and injects matching ecosystem guidelines in Spanish or English into the prompt context and rule-based suggestions -> Keeps prompt improvement aligned with our premium, specialized developer skills.
- [2026-07-14]: Cache has TWO eviction axes -> TTL (`prune_expired`, time-based, every save) + size cap (`prune_over_capacity`, LRU oldest-by-mtime, `CACHE_MAX_ENTRIES` default 500, env `OLLAMA_IMPROVE_CACHE_MAX_ENTRIES`; cap<=0 disables). TTL alone left the on-disk cache unbounded under long TTL + many distinct prompts; the cap bounds growth. -> TTL-only (grows without bound).
- [2026-07-14]: Optional metrics via `OLLAMA_IMPROVE_METRICS=1` (or DEBUG) -> in-memory Counter in `shared/metrics.py` recorded at decision points (cache:hit / ollama / cloud fresh, passthrough:trivial|concrete|noimprove, error, source) and emitted to stderr once per invocation from `main()` try/finally. `record()` always tallies (cheap dict op); `emit()` short-circuits unless enabled, so the disabled hot path (the normal case — the hook is subprocess-per-prompt) is untouched. -> persistent store / always-on stderr (both waste I/O on the hot path).
- [2026-07-14]: `PROMPT_IMPROVE_SHAPE_BY` (model|cli, default model) -> decides whether prompt-shape follows the real MODEL family (default — a Claude-Code-on-GLM proxy shapes for GLM) or the CLI family (`cli` — shapes for Claude XML even under a proxy). `_CLI_FAMILY_STYLE` dict is the single source of truth for cli->(family,style), shared by profile_for_model's fallback branch and profile_for_cli. -> hardcoding model-wins (not reversible for users who want CLI shaping).
- [2026-07-14]: Rule-based fallback (`source=="fallback:rules"`) is NEVER a rewrite -> rules always yield clarify-style bullets; treating effective_mode=="rewrite" as authoritative dropped the user's original prompt in direct-CLI mode. is_rewrite excludes fallback:rules explicitly. -> trusting effective_mode when the LLM failed and rules substituted (it lies).
- [2026-07-14]: Test env isolation for target detection scrubs by PREFIX (CODEX_*, CLAUDE_CODE_*) not just named vars -> `_cli_from_env_or_model` short-circuits to 'codex' on ANY residual CODEX_* before checking claude markers, so one unscrubbed `CODEX_HOME`/etc. misclassifies. Named-var-only scrub (the original tests) was flaky under any real harness env. -> named-var-only scrub.
- [2026-07-14]: paths.py imports detect_language LOCALLY (inside `_ecosystem_skill_hint`) -> detect.py imports paths at module top (`_BARE_CONTINUATION_RE`), so a module-level `from detect import ...` in paths would cycle. A function-local import is safe because by call time both modules are fully loaded. -> module-level import (ImportError at load).


- 2026-07-14T23:11:52Z | - [2026-07-14]: DeepSeek-R1 modeled as distinct reasoner lineage -> R1/deepseek-reasoner is a PURE reasoning model (no system prompt, zero-shot, temp 0.5-0.7/0.6 rec, no few-shot, no requested visible CoT) per official DeepSeek-R1 README; previously _deepseek_style + SHAPES[deepseek] treated ALL deepseek as reasoning, conflating R1 with V3/V4 instruct. Fix: profile._deepseek_style flags 'r1'/'reasoner' -> style 'deepseek-r1-reasoner' (family stays 'deepseek'); shape._variant_guidance adds R1 branch with README constraints BEFORE the v4 branch. Additive; base V4 behavior text untouched (tests pin 'reasoning' keyword + 'do not request hidden chain-of-thought' verbatim for v4-flash). +2 tests. -> Rewriting base SHAPES[deepseek] to be instruct-neutral (would break the v4-flash verbatim assertions and over-reach into V3 which isn't the gap). discovered via 2nd-review research for prompt-engineer skill v2.1.0.
- 2026-07-17T15:51:29Z | status:completed | Optional metrics persistence is fail-fast under lock contention and portable without fcntl; corrupt cache eviction compares file identity before unlink so a concurrent atomic replacement is never deleted.
- [2026-07-18]: Native Ubuntu host — Ollama autostart prefers systemd (`systemctl --user` then system unit) before nohup `ollama serve` -> keeps supervision/logging/GPU env with the managed unit; rogue second serve competed on native desktop. WSL dual-daemon history is obsolete on this machine. -> nohup-only (fine for containers without a unit).
- [2026-07-18]: Grok/xAI is a first-class target family (not generic Markdown) -> Grok Build sessions export `GROK_AGENT` and run the shared UserPromptSubmit pipeline; without detection the improver wasted the shaping path every other CLI already had. Format=GFM Markdown; behavior=cap scope under always-approve/yolo. Env: `GROK_AGENT`/`GROK_MODEL`/`XAI_MODEL` + cli aliases grok/grok-build/xai. -> treating Grok as generic (silent quality drop).
- [2026-07-18]: Improve available-model tail filters non-chat tags (`embed*`, `bge-*`, `nomic-embed`, …) -> preferred chain miss used to append every `/api/tags` name including embeddings and burn the 20s wall budget. Explicit chain overrides still win if the user names an embedding (rare). -> unfiltered tail.
